import 'package:flutter/material.dart';

class AppStyles {
  static const neonGreen = Color(0xFF00E676);
  static const primaryTextColor = Colors.white;
  static const secondaryTextColor = Colors.white70;
  static const translucentBlack = Colors.black54;
  static const subtleBorder = Colors.white12;
  static const monospacedStyle = TextStyle(
    fontFamily: 'monospace',
    color: primaryTextColor,
    fontWeight: FontWeight.bold,
  );
}